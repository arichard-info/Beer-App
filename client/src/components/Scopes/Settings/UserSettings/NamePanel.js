import React from "react";
import styled, { css } from "styled-components";

const NamePanel = ({ className }) => {
  return <div className={className}></div>;
};

export default styled(NamePanel)(() => css``);
