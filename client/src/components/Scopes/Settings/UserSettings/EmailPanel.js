import React from "react";
import styled, { css } from "styled-components";

const EmailPanel = ({ className, email }) => {
  return <div className={className}></div>;
};

export default styled(EmailPanel)(() => css``);
